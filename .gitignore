.DS_Store
__pycache__/
*.pyc
.venv/
node_modules/
.gstack/
data/
*.db
*.db-journal
*.db-wal
*.db-shm
.env
.env.*-backup
.env.polluted-backup
runs/
.coverage
htmlcov/
*.egg-info/
static/
web/node_modules/
graphify-out/

# workflow halt 临时输出（OSCILLATING/blocked.md，halt 后过时）
.workflow/

# AUDIT 阶段报告（refactor 类 task Pre-RED 核查，每次重跑覆盖写，不入版本控制）
.audit/

# SDD (subagent-driven-development) scratch files（task briefs/reports/review-diff packages/progress ledger，session 内派生物，跨 session 不保留）
.superpowers/

# Playwright MCP 临时页面快照（浏览外部文档时的 accessibility tree dump，一次性调试产物）
.playwright-mcp/

# workflow 引擎（私有内部工具，经 scripts/setup-workflow-engine.sh 按需 clone，不入库）
.claude/workflow-engine/

# NAS 专属运维记录（内网细节，本地保留，不入库）
deploy-nas-internal.md

# 自签证书（本地生成，HTTPS 部署用）
certs/
